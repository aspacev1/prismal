name: flowline-app

services:
  app:
    image: flowline-app-dev
    build:
      context: .
      target: dev
    command: sh -c "npx prisma generate && npx prisma migrate deploy && npm run dev"
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      # .env's DATABASE_URL points at "localhost" for host-side `npm run
      # dev`/`npm test`. Inside this container, Postgres is reachable at
      # the "postgres" service hostname instead — override it here rather
      # than making .env wrong for the host-side case.
      DATABASE_URL: postgresql://flowline:flowline@postgres:5432/flowline
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: flowline
      POSTGRES_PASSWORD: flowline
      POSTGRES_DB: flowline
    ports:
      # Host 5433 → container 5432. Avoids colliding with a Postgres already
      # running on the host's default 5432; containers still talk to each
      # other over 5432 internally (see app's DATABASE_URL above).
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
